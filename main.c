#include <stdio.h>
#include <string.h>
char s[1000];
void split(char c[])
{
    int n,i;
    n=strlen(c);
    for(i=1;i<=n;i++);
}
int main()
{
    printf("请输入待计算的字符串。帮助函数为help()，返回值为0。");
    scanf("%s",&s);
    split(s);
    return 0;
}
